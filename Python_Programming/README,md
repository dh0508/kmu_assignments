1. 기초 문법·OOP: 날짜/숫자 입력 파싱, 리스트·거듭제곱 연산, 클래스 상속, 모듈 임포트 실습.
2. 분기·반복 심화: 최댓값 계산, 다중조건(if/elif), 구구단(중첩루프), for vs while, continue, match-case.
3. 함수·알고리즘: 다중 반환(튜플), 재귀 vs 동적계획(Fibonacci), 반올림 구현, 이진수 판별·입력 검증.
4. 응용: 난수 생성(로또), 리스트로 스택 구현, 메뉴/검색 입력 검증, 캡슐화(getter/setter)·상속/오버라이딩, 모듈 사용.
